import React from "react";
import { View, Text } from "react-native";
import { deviceWidth, deviceHeight } from "../../utils/Dimensions"



export default function Header() {
  
  
    return (
        <View style={{ height: 304, width: deviceWidth }}>
            
        </View>
    )
}